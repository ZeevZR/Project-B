name: Generate ASCII Artwork 
on: push 

jobs: 
    ascii_job:
       runs-on: ubuntu-latest

       steps:
       - name: Checkout Repo
         uses: actions/checkout@v6

       - name: list Repo Files 
         run: ls -ltra  

       - name: Executing Shell script
         run: |
           chmod +x ./ascii-scipt.sh
           ./ascii-scipt.sh
        